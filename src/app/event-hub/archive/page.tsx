import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Eventogy - Event Hub - Archive",
  description: "",
};

export default function ArchivePage() {
  return <>Archive</>;
}
